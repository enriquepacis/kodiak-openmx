# -*- coding: utf-8 -*-
# -*- mode: org -*-

#+title: dev.org

#+STARTUP: overview indent


* Overview

This is an Emacs org notebook where I keep notes, develop the code and perform
GitHub tasks.

* Source Code


* GitHub                                                       :noexport:

This is executable Emacs org-mode code that performs GitHub tasks.
** Create new Repository

#+begin_src zsh :results output
  source ~/.zshrc
  git init
  git add README.org
  git commit -m "first commit"
  git branch -M main
  git remote add origin https://github.com/enriquepacis/kodiak-openmx.git
  git push -u origin main
#+end_src

#+RESULTS:
: Initialized empty Git repository in /Users/enrique_blair/Library/CloudStorage/Box-Box/Research/2026/kodiak-openmx/.git/
: [main (root-commit) 2cae4c1] first commit
:  1 file changed, 24 insertions(+)
:  create mode 100644 README.org
: branch 'main' set up to track 'origin/main'.
** Sync Local-Remote Repository

#+begin_src zsh :results output
  source ~/.zshrc
  cd .. # navigate to main directory
  git add dev/dev.org
  git commit -m "Adds Shell Tasks to dev.org"
  git pull
  git push
#+end_src

#+RESULTS:
: [main 95b4ec5] Moves executable code blocks from README to dev.org.
:  1 file changed, 1 insertion(+), 35 deletions(-)
: Already up to date.

* Shell Tasks
#+begin_src zsh :results output
  pwd
  ls *.org
  # mkdir dev
  # mv dev.org dev
  # ls dev
  mkdir src
  mkdir src/kodiak_openmx
#+end_src

#+RESULTS:
: /Users/enrique_blair/Library/CloudStorage/Box-Box/Research/2026/kodiak-openmx
: README.org
